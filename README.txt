The structure of the repository:

prototype/
    The python code for prototyping and running experiments.

evaluation/
    The C++ app for fast implementations of algorithms (currently only used for creating training datasets for the ANN-distance heuristic).

utils/
    Some utilities for working with datasets.

data/
    The directory that stores the calculated pattern databases, saved neural networks, and datasets with experiments.
